import * as types from '../Actions/types'

const initialState = {

}

export default function ModuleReducer(state = initialState,action) {
    switch(action.type) {
        default:
            return state;
    }
}